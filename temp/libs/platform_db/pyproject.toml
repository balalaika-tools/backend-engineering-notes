[project]
name = "platform-db"
version = "0.1.0"
requires-python = ">=3.13,<3.14"
dependencies = [
    "sqlmodel>=0.0.31,<0.1",
]

[build-system]
requires = ["hatchling>=1.32.0,<2"]
build-backend = "hatchling.build"
